          </div>
        </div>
      )}

      {/* Document Upload Modal */}
      {showDocUploadModal && (
        <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4">
          <div className="bg-white rounded-lg shadow-xl max-w-4xl w-full max-h-[90vh] overflow-y-auto">
            {/* Modal Header */}
            <div className="sticky top-0 bg-gradient-to-r from-emerald-600 to-teal-600 text-white px-6 py-4 flex justify-between items-center">
              <div>
                <h2 className="text-xl font-bold">Upload Engineering Documents</h2>
                <p className="text-sm text-emerald-100 mt-1">Upload supporting documents for line list data enrichment</p>
              </div>
              <button
                onClick={() => setShowDocUploadModal(false)}
                className="text-white hover:text-emerald-100 p-2"
              >
                <XCircleIcon className="w-6 h-6" />
              </button>
            </div>

            {/* Modal Body */}
            <div className="p-6 space-y-6">
              {/* Hidden File Inputs */}
              <input type="file" ref={hmbFileRef} accept=".xlsx,.xls,.pdf" onChange={(e) => handleDocumentUpload(e, 'hmb')} className="hidden" />
              <input type="file" ref={pfdFileRef} accept=".pdf" onChange={(e) => handleDocumentUpload(e, 'pfd')} className="hidden" />
              <input type="file" ref={pmsFileRef} accept=".pdf" onChange={(e) => handleDocumentUpload(e, 'pms')} className="hidden" />
              <input type="file" ref={designBasisFileRef} accept=".pdf" onChange={(e) => handleDocumentUpload(e, 'design_basis')} className="hidden" />
              <input type="file" ref={naceFileRef} accept=".pdf" onChange={(e) => handleDocumentUpload(e, 'nace')} className="hidden" />
              <input type="file" ref={weldingFileRef} accept=".pdf" onChange={(e) => handleDocumentUpload(e, 'welding')} className="hidden" />
              <input type="file" ref={pressureTestFileRef} accept=".pdf" onChange={(e) => handleDocumentUpload(e, 'pressure_test')} className="hidden" />
              <input type="file" ref={insulationFileRef} accept=".pdf" onChange={(e) => handleDocumentUpload(e, 'insulation')} className="hidden" />

              {/* Process Documents Section */}
              <div className="border border-blue-200 rounded-lg p-4 bg-blue-50">
                <h3 className="text-lg font-semibold text-blue-900 mb-3 flex items-center">
                  <span className="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm mr-2">1</span>
                  ðŸ“‹ Process Documents
                </h3>
                <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
                  {/* HMB */}
                  <button
                    onClick={() => hmbFileRef.current?.click()}
                    disabled={uploadingDocs.hmb}
                    className="flex items-center justify-between p-4 bg-white border-2 border-blue-300 rounded-lg hover:bg-blue-50 hover:border-blue-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.hmb ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-blue-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-blue-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900">Heat & Material Balance</div>
                        <div className="text-xs text-gray-500">Excel or PDF</div>
                      </div>
                    </div>
                    {docUploadResults.hmb && (
                      docUploadResults.hmb.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>

                  {/* PFD */}
                  <button
                    onClick={() => pfdFileRef.current?.click()}
                    disabled={uploadingDocs.pfd}
                    className="flex items-center justify-between p-4 bg-white border-2 border-blue-300 rounded-lg hover:bg-blue-50 hover:border-blue-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.pfd ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-blue-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-blue-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900">PFD Stream Tables</div>
                        <div className="text-xs text-gray-500">PDF</div>
                      </div>
                    </div>
                    {docUploadResults.pfd && (
                      docUploadResults.pfd.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>
                </div>
              </div>

              {/* Piping Specifications Section */}
              <div className="border border-purple-200 rounded-lg p-4 bg-purple-50">
                <h3 className="text-lg font-semibold text-purple-900 mb-3 flex items-center">
                  <span className="bg-purple-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm mr-2">2</span>
                  ðŸ”§ Piping Specifications
                </h3>
                <div className="grid grid-cols-1 md:grid-cols-3 gap-3">
                  {/* PMS */}
                  <button
                    onClick={() => pmsFileRef.current?.click()}
                    disabled={uploadingDocs.pms}
                    className="flex items-center justify-between p-4 bg-white border-2 border-purple-300 rounded-lg hover:bg-purple-50 hover:border-purple-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.pms ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-purple-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-purple-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900 text-sm">Piping Material Specs</div>
                        <div className="text-xs text-gray-500">PDF</div>
                      </div>
                    </div>
                    {docUploadResults.pms && (
                      docUploadResults.pms.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>

                  {/* Design Basis */}
                  <button
                    onClick={() => designBasisFileRef.current?.click()}
                    disabled={uploadingDocs.design_basis}
                    className="flex items-center justify-between p-4 bg-white border-2 border-purple-300 rounded-lg hover:bg-purple-50 hover:border-purple-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.design_basis ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-purple-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-purple-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900 text-sm">Design Basis</div>
                        <div className="text-xs text-gray-500">PDF</div>
                      </div>
                    </div>
                    {docUploadResults.design_basis && (
                      docUploadResults.design_basis.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>

                  {/* NACE */}
                  <button
                    onClick={() => naceFileRef.current?.click()}
                    disabled={uploadingDocs.nace}
                    className="flex items-center justify-between p-4 bg-white border-2 border-purple-300 rounded-lg hover:bg-purple-50 hover:border-purple-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.nace ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-purple-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-purple-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900 text-sm">NACE Report</div>
                        <div className="text-xs text-gray-500">PDF</div>
                      </div>
                    </div>
                    {docUploadResults.nace && (
                      docUploadResults.nace.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>
                </div>
              </div>

              {/* Fabrication & Test Specs Section */}
              <div className="border border-orange-200 rounded-lg p-4 bg-orange-50">
                <h3 className="text-lg font-semibold text-orange-900 mb-3 flex items-center">
                  <span className="bg-orange-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm mr-2">3</span>
                  âš™ï¸ Fabrication & Test Specs
                </h3>
                <div className="grid grid-cols-1 md:grid-cols-3 gap-3">
                  {/* Welding */}
                  <button
                    onClick={() => weldingFileRef.current?.click()}
                    disabled={uploadingDocs.welding}
                    className="flex items-center justify-between p-4 bg-white border-2 border-orange-300 rounded-lg hover:bg-orange-50 hover:border-orange-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.welding ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-orange-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-orange-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900 text-sm">Welding / PWHT / NDE</div>
                        <div className="text-xs text-gray-500">PDF</div>
                      </div>
                    </div>
                    {docUploadResults.welding && (
                      docUploadResults.welding.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>

                  {/* Pressure Test */}
                  <button
                    onClick={() => pressureTestFileRef.current?.click()}
                    disabled={uploadingDocs.pressure_test}
                    className="flex items-center justify-between p-4 bg-white border-2 border-orange-300 rounded-lg hover:bg-orange-50 hover:border-orange-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.pressure_test ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-orange-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-orange-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900 text-sm">Pressure Test Spec</div>
                        <div className="text-xs text-gray-500">PDF</div>
                      </div>
                    </div>
                    {docUploadResults.pressure_test && (
                      docUploadResults.pressure_test.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>

                  {/* Insulation */}
                  <button
                    onClick={() => insulationFileRef.current?.click()}
                    disabled={uploadingDocs.insulation}
                    className="flex items-center justify-between p-4 bg-white border-2 border-orange-300 rounded-lg hover:bg-orange-50 hover:border-orange-500 transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                  >
                    <div className="flex items-center">
                      {uploadingDocs.insulation ? (
                        <div className="animate-spin rounded-full h-5 w-5 border-2 border-orange-600 border-t-transparent mr-3" />
                      ) : (
                        <ArrowUpTrayIcon className="w-5 h-5 text-orange-600 mr-3" />
                      )}
                      <div className="text-left">
                        <div className="font-medium text-gray-900 text-sm">Thermal Insulation</div>
                        <div className="text-xs text-gray-500">PDF</div>
                      </div>
                    </div>
                    {docUploadResults.insulation && (
                      docUploadResults.insulation.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600" />
                      )
                    )}
                  </button>
                </div>
              </div>

              {/* Upload Results Messages */}
              {Object.keys(docUploadResults).length > 0 && (
                <div className="mt-4 space-y-2">
                  {Object.entries(docUploadResults).map(([docType, result]) => result && (
                    <div
                      key={docType}
                      className={`p-3 rounded-lg flex items-start ${
                        result.success ? 'bg-green-50 border border-green-200' : 'bg-red-50 border border-red-200'
                      }`}
                    >
                      {result.success ? (
                        <CheckCircleIcon className="w-5 h-5 text-green-600 mr-2 mt-0.5 flex-shrink-0" />
                      ) : (
                        <XCircleIcon className="w-5 h-5 text-red-600 mr-2 mt-0.5 flex-shrink-0" />
                      )}
                      <div className="flex-1">
                        <p className={`text-sm font-medium ${
                          result.success ? 'text-green-900' : 'text-red-900'
                        }`}>
                          {result.message}
                        </p>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>

            {/* Modal Footer */}
            <div className="bg-gray-50 px-6 py-4 flex justify-end">
              <button
                onClick={() => setShowDocUploadModal(false)}
                className="px-6 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition-colors"
              >
                Close
              </button>
            </div>
          </div>
        </div>
      )}

